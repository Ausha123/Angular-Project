import { StringDividerPipe } from './string-divider.pipe';

describe('StringDividerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringDividerPipe();
    expect(pipe).toBeTruthy();
  });
});
